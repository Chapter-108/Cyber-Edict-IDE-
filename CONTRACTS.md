# 类型契约（阶段 0）

对 `src/types/agent.ts`（`AgentNodeData` / `AgentFlowData`）与 `src/types/workflow.ts`（`WorkflowTask` / `TaskStatus`）的**破坏性变更**需经评审并同步本文件版本记录。

当前版本：**v0.2.0** — 合并子项目任务流（下旨 → 门下省审议）与 ROADMAP §3。

`src/types/llm.ts`（`LlmSettings`）与 `useLlmSettingsStore` 持久化字段变更时建议同步说明。
